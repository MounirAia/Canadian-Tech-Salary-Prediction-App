[[source]]
url = "https://pypi.org/simple"
verify_ssl = true
name = "pypi"

[scripts]
dev = "pipenv run flask --app app run --debug"

[packages]
flask = "*"
python-dotenv = "*"
pymongo = "*"
flask-cors = "*"

[dev-packages]

[requires]
python_version = "3.10"
